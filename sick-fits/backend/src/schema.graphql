# import * from './generated/prisma.graphql'

type Mutation {
  createItem(
    title: String!
    discription: String
    price: Int
    image: String
    largeImage: String
  ): Item!
}

type Query {
  items: [Item]!
}
